Drop the two avatar videos here, named exactly:

  rest.mp4      (idle/listening loop)
  talking.mp4   (speaking loop)

Both should show the same girl + same environment/background, ideally the
same resolution and frame rate, so the crossfade between them looks seamless.
